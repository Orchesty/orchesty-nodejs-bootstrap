# Orchesty Node.js Bootstrap

## How to install
1. Clone bootstrap repository `https://github.com/Orchesty/orchesty-nodejs-bootstrap`
2. Initialize a git project from cloned template `rm -rf .git && git init`

## How to run
- Run `make init-dev` (local) or `make init-dev-docker` (docker)
